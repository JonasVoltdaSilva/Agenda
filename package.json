{
  "name": "agenda",
  "version": "1.0.0",
  "private": true,
  "description": "Agenda — organização pessoal estilo Bullet Journal (PWA, React via CDN, JSX pré-compilado)",
  "scripts": {
    "build": "node build.js"
  },
  "devDependencies": {
    "@babel/core": "^7.25.0",
    "@babel/preset-react": "^7.24.0"
  }
}
